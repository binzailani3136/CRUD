# CRUD Boilerplate
Node+Express+Mongo+GraphQL CRUD with authentication boilerplate

##Installation
`npm install --no-optional`

##Before you run
###Init Databse
`npm run initdb`
###Drop Database
`npm run dropdb`
###Reset Databse
`npm run revert`

##Run Server
`npm start`
